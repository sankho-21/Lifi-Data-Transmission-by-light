# Lifi-Data-Transmission-by-light
using an arduino and an raspberry pi, a system that takes some data as input, converts it to binary then transmits it using light and receives it, decrypts it accordingly.
